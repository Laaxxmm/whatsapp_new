# Base URL of the Indefine Chat MCP API (note the /api/mcp/v1 suffix).
INDEFINE_API_URL=https://crm.srv879786.hstgr.cloud/api/mcp/v1

# A bearer key minted in Indefine Chat → Admin Settings → MCP Tools (shown once).
INDEFINE_API_KEY=fck_live_PASTE_YOUR_KEY
